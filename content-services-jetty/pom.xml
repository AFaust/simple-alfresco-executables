<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>de.axelfaust.experiment</groupId>
        <artifactId>simple-alfresco-executables</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>de.axelfaust.experiment.content.services.jetty</artifactId>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.eclipse.jetty</groupId>
                <artifactId>jetty-server</artifactId>
                <version>${dep.jetty.version}</version>
            </dependency>

            <dependency>
                <groupId>de.axelfaust.experiment</groupId>
                <artifactId>de.axelfaust.experiment.generic.jetty</artifactId>
                <version>${project.version}</version>
                <classifier>shaded</classifier>
                <exclusions>
                    <exclusion>
                        <groupId>*</groupId>
                        <artifactId>*</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <dependency>
                <groupId>de.axelfaust.experiment</groupId>
                <artifactId>de.axelfaust.experiment.content.services.uberjar</artifactId>
                <version>${project.version}</version>
                <classifier>shaded</classifier>
                <exclusions>
                    <exclusion>
                        <groupId>*</groupId>
                        <artifactId>*</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>

            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${dep.jdbc.postgresql.version}</version>
                <scope>runtime</scope>
            </dependency>

            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <version>${dep.jdbc.mysql.version}</version>
                <scope>runtime</scope>
            </dependency>

            <dependency>
                <groupId>org.alfresco</groupId>
                <artifactId>alfresco-share-services</artifactId>
                <version>${dep.alf.share.version}</version>
                <classifier>classes</classifier>
                <scope>runtime</scope>
            </dependency>
            
            <dependency>
                <groupId>org.orderofthebee.support-tools</groupId>
                <artifactId>support-tools-repo</artifactId>
                <version>${dep.ootbee.support-tools.version}</version>
                <classifier>classes</classifier>
                <scope>runtime</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.eclipse.jetty</groupId>
            <artifactId>jetty-server</artifactId>
        </dependency>
        <dependency>
            <groupId>de.axelfaust.experiment</groupId>
            <artifactId>de.axelfaust.experiment.generic.jetty</artifactId>
            <classifier>shaded</classifier>
        </dependency>
        <dependency>
            <groupId>de.axelfaust.experiment</groupId>
            <artifactId>de.axelfaust.experiment.content.services.uberjar</artifactId>
            <classifier>shaded</classifier>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
        </dependency>
        <dependency>
            <groupId>org.alfresco</groupId>
            <artifactId>alfresco-share-services</artifactId>
            <classifier>classes</classifier>
        </dependency>
        <dependency>
            <groupId>org.orderofthebee.support-tools</groupId>
            <artifactId>support-tools-repo</artifactId>
            <classifier>classes</classifier>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>

                <plugin>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <executions>
                        <execution>
                            <id>unpack-webapps</id>
                            <phase>prepare-package</phase>
                            <goals>
                                <goal>unpack</goal>
                            </goals>
                            <configuration>
                                <artifactItems>
                                    <artifactItem>
                                        <groupId>org.alfresco</groupId>
                                        <artifactId>alfresco-server-root</artifactId>
                                        <version>${dep.alf.serverRoot.version}</version>
                                        <type>war</type>
                                        <outputDirectory>${project.build.outputDirectory}/webapps/ROOT</outputDirectory>
                                    </artifactItem>
                                    <artifactItem>
                                        <groupId>org.alfresco</groupId>
                                        <artifactId>content-services-community</artifactId>
                                        <version>${dep.alf.content-services.version}</version>
                                        <type>war</type>
                                        <outputDirectory>${project.build.outputDirectory}/webapps/alfresco</outputDirectory>
                                        <excludes>WEB-INF/lib/*.jar,WEB-INF/classes/log4j.properties</excludes>
                                    </artifactItem>
                                    <artifactItem>
                                        <groupId>org.alfresco</groupId>
                                        <artifactId>alfresco-share-services</artifactId>
                                        <version>${dep.alf.share.version}</version>
                                        <type>amp</type>
                                        <!-- install directly into exploded webapp -->
                                        <outputDirectory>${project.build.outputDirectory}/webapps/alfresco</outputDirectory>
                                        <excludes>lib/*.jar</excludes>
                                    </artifactItem>
                                    <artifactItem>
                                        <groupId>org.orderofthebee.support-tools</groupId>
                                        <artifactId>support-tools-repo</artifactId>
                                        <version>${dep.ootbee.support-tools.version}</version>
                                        <type>amp</type>
                                        <!-- install directly into exploded webapp -->
                                        <outputDirectory>${project.build.outputDirectory}/webapps/alfresco</outputDirectory>
                                        <excludes>lib/*.jar</excludes>
                                    </artifactItem>
                                </artifactItems>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <artifactId>maven-shade-plugin</artifactId>
                    <executions>
                        <execution>
                            <phase>package</phase>
                            <goals>
                                <goal>shade</goal>
                            </goals>
                            <configuration>
                                <shadedArtifactAttached>true</shadedArtifactAttached>
                                <shadedClassifierName>shaded</shadedClassifierName>
                                <artifactSet>
                                    <includes>
                                        <include>de.axelfaust.experiment:de.axelfaust.experiment.generic.jetty:*</include>
                                        <include>de.axelfaust.experiment:de.axelfaust.experiment.content.services.uberjar:*</include>
                                        <include>org.postgresql:*</include>
                                        <include>mysql:mysql-connector-java</include>
                                        <include>org.alfresco:alfresco-share-services:classes</include>
                                        <include>org.orderofthebee.support-tools:support-tools-repo:classes</include>
                                    </includes>
                                </artifactSet>
                                <filters>
                                    <filter>
                                        <artifact>*:*</artifact>
                                        <excludes>
                                            <exclude>META-INF/*.SF</exclude>
                                            <exclude>META-INF/*.DSA</exclude>
                                            <exclude>META-INF/*.RSA</exclude>
                                        </excludes>
                                    </filter>
                                    <filter>
                                        <artifact>de.axelfaust.experiment:de.axelfaust.experiment.content.services.uberjar</artifact>
                                        <excludes>
                                            <exclude>org/objectweb/asm/**</exclude>
                                            <exclude>net/sf/cglib/**</exclude>
                                        </excludes>
                                        <!-- TODO Need to relocate all static files from alfresco/* to webapps/alfresco/WEB-INF/classes/alfresco/ -->
                                    </filter>
                                </filters>
                                <transformers>
                                    <transformer
                                        implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                        <mainClass>de.axelfaust.experiment.content.services.jetty.Runner</mainClass>
                                    </transformer>
                                    <transformer
                                        implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
                                    <transformer implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
                                        <resource>META-INF/spring.handlers</resource>
                                    </transformer>
                                    <transformer implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
                                        <resource>META-INF/spring.schemas</resource>
                                    </transformer>
                                    <transformer implementation="org.apache.maven.plugins.shade.resource.AppendingTransformer">
                                        <resource>META-INF/spring.tooling</resource>
                                    </transformer>
                                </transformers>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <artifactId>maven-enforcer-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-shade-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>